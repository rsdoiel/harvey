CONTINUE NEXT TIME — Harvey harness-engineering work
Last session: 2026-07-13

WHAT THIS THREAD OF WORK IS
---------------------------
Applying Birgitta Boeckeler's harness-engineering framework (Agent = Model +
Harness; guides=feedforward vs sensors=feedback; computational vs
inferential) to Harvey's own codebase. Source material and the full
direction list live in:

    harvey/harness-engineering-exploration.md

That file is the single source of truth for Directions A-F. Don't duplicate
its content here — read it directly when picking up this thread.

WHAT'S DONE
-----------
1. Pre-harness-engineering refactor cycle (3 items, all complete):
   - harvey/harness-prerequisite-refactor-design.md
   - harvey/harness-prerequisite-refactor-plan.md
   - Decisions logged in harvey/DECISIONS.md (three 2026-07-12 entries)

2. Direction A (computational guide/sensor tools) — first increment complete:
   - harvey/computational-sensors-design.md
   - harvey/computational-sensors-plan.md
   - Decisions logged in harvey/DECISIONS.md (two more 2026-07-12 entries)
   - Shipped: Agent.ActiveStatus + SensorEvent plumbing, CodeFormatter.Check()
     wired into a real sensor, go vet as the first genuinely new sensor.
   - Deliberately deferred within Direction A itself: staticcheck/gocyclo,
     unused-param check, changed-file-without-changed-test heuristic,
     hunspell/grammar checking. Not built. Candidates only.

3. Direction B (lint the feedforward side) — both phases complete:
   - harvey/feedforward-budget-design.md
   - harvey/feedforward-budget-plan.md
   - Decisions logged in harvey/DECISIONS.md (two 2026-07-13 entries)
   - Shipped: BudgetTracker shared across ragAugment + injectOrChunk (one
     per-turn pool instead of three uncoordinated checks); a real regression
     found and fixed along the way (unknown context limit → zero budget →
     all injection silently blocked); a cause-neutral gofmt Check() message
     after finding the original "likely a syntax error" wording would be
     wrong under Config.AutoFormat=false.
   - Item 1 (content-aware guide selection) explicitly out of scope — no
     path-scoped skill exists yet in Harvey's own catalog to gate.

4. Direction D (one subagent-dispatch primitive) — all four phases complete:
   - harvey/subagent-dispatch-design.md
   - harvey/subagent-dispatch-plan.md
   - Decisions logged in harvey/DECISIONS.md (four 2026-07-13 entries)
   - Auditing /plan next, /read-chunks+injectOrChunk, and @mention before
     designing found two real, previously-uncaught bugs, not just
     duplicated code: (1) @mention in a chunk-analysis instruction only
     relabeled the session recording, never actually dispatched to the
     named model; (2) /plan next's post-step model restore read a config
     field the step's own switch had already overwritten, so it never
     actually restored the pre-step model. Both fixed via one
     resolveDispatchTarget primitive (Phases A-B) — each regression test
     verified to fail against the pre-fix code before confirming the fix.
     Phases C-D removed two further real duplications (runBoundedTurn,
     foldBackTurn), each verified behavior-preserving via unmodified
     pre-existing tests, not just new tests passing.
   - @mention's local-model-switch fallthrough and chunk analysis's
     tool-free design were both explicitly audited and left out of scope
     (genuinely different mechanisms, not consolidation targets).

5. Direction E (guide-vs-sensor token-cost comparison) — mechanism built,
   no real experiment run yet, by design:
   - harvey/guide-compare-design.md
   - harvey/guide-compare-plan.md
   - Decision logged in harvey/DECISIONS.md (one 2026-07-13 entry)
   - Auditing cmd/assay/main.go before designing found it sends no system
     prompt at all today (every dispatch is a bare user message) — the
     exploration doc's framing assumed that plumbing already existed.
     Also found no guide/sensor pair currently exists in Harvey to test:
     HARVEY.md's guide sections don't correspond to either of Direction
     A's two shipped sensors (gofmt Check(), go vet).
   - Shipped: --guide-compare + --guide-file flags on assay, mirroring
     --rag-compare's shape exactly (no corpus-format changes needed —
     answers the exploration doc's own open question). The guide is an
     opaque external file, not derived from Harvey's own HARVEY.md/skills
     assembly, respecting cmd/assay's "otherwise self-contained" boundary.
   - No real "does removing this guide hurt" experiment was run —
     deliberately deferred, confirmed with the user, rather than
     manufacturing a guide or sensor purely to have a subject.

6. Direction F (harness-template skills) — audited, resolved as
   blocked-not-pursued, no code changes:
   - Decision logged in harvey/DECISIONS.md (one 2026-07-13 entry)
   - Auditing commands_skill.go's skillLoad (the /skill load
     implementation) before designing found it does exactly one thing —
     injects the skill's markdown body into context. No hook exists for a
     skill to provision anything else into the workspace.
   - More fundamentally: no sensor in Harvey today is config-driven at
     all. gofmt Check() and go vet (Direction A's only two shipped
     sensors) are both hardcoded; Config's only sensor-related field
     (SensorInjectFormatFindings) is a visibility bool, not a ruleset.
     "Scaffold sensor config alongside guide markdown" has no artifact to
     scaffold, independent of how many sensors exist — a more fundamental
     gap than "not enough sensors yet."
   - Confirmed with the user: stop here, document, build nothing. Revisit
     only if/when a config-driven sensor exists in Harvey for independent
     reasons — at that point F would have a first real artifact and
     Direction E's "mechanism only" pattern likely applies.

**This closes out the harness-engineering-exploration.md thread.**
Directions A-F are now each either implemented (A, B, D, E) or explicitly
resolved as blocked-not-pursued with the reason documented (F).

Full test suite is green except one pre-existing, unrelated failure
(TestCmdModelList_ShowsLlamafileEntries — tied to already-uncommitted
llamafile-discovery work in llamafile.go/backend_startup.go that predates
this whole thread; not caused by anything above).

WHAT'S OPEN
-----------
Nothing from harness-engineering-exploration.md's Directions A-F remains
actionable without a new prerequisite existing first:

- A real first Direction E experiment needs either a new guide-vs-sensor
  pair or one to emerge naturally from other work. The most plausible
  candidate noted (not built): HARVEY.md's "Documentation conventions"
  section (doc-comment presence) paired with a new doc-comment-presence
  sensor — neither exists yet.
- Direction F needs a config-driven sensor to exist first (see above) —
  not itself scheduled anywhere.

commands.go (3877 lines, 71 functions) remains explicitly out of scope for
all of this — real debt, doesn't block any harness-engineering direction,
tracked separately if it's ever addressed.

SUGGESTED STARTING POINT (a suggestion, not a decision already made)
---------------------------------------------------------------------
This thread has no more directly-actionable work from
harness-engineering-exploration.md without first creating a prerequisite
(a real guide/sensor pair for E, a config-driven sensor for F) that doesn't
exist yet for its own sake. If picking this up again, the natural entry
point is deciding whether either prerequisite is worth building
deliberately, or waiting for one to arise from unrelated work first.

If a different priority has come up since this was written, ignore this
section — it reflects reasoning as of 2026-07-13, not a standing plan.

HOW TO PICK THIS UP
-------------------
Whichever direction is chosen needs its own design -> decisions -> planning
cycle before implementation, same as every item in this thread so far: a
<topic>-design.md auditing what's actually in the code first (not assumed),
explicit decisions confirmed before coding, a <topic>-plan.md, then
TDD-first implementation with each decision logged in DECISIONS.md. Every
increment in this thread so far has turned up at least one real, previously
unknown finding during the audit step (a latent bug, a stale premise, a
design-vs-implementation gap) — the audit is not a formality.

Cross-referenced findings from this whole thread are also recorded in
agents/knowledge.db (project_id=1, name='harvey') as a chronological set of
'decision' observations dated 2026-07-12 and 2026-07-13 — useful for a quick
narrative recap without re-reading every design doc in full.
