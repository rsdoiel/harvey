
# Ollama Models

So far the best results I've have for single model has been phi4-mini:latest. It does produce simple code and can run over a prompt in about 15 minutes. I still need to test the currenly installed models.

phi3.5 might be good to do dispatch though I think phi4-mini might be performant enough. 

I am on the fense about gemma2:latest has a very small context window, will need to learn to skope to its capabilities.

The tinyllama model was not able to integrate with tools at all. So I probavbly should skip it.

I need to come up with recommendations for single model usage and multi-model usage.
